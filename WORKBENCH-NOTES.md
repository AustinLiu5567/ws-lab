# 2026-09-22 更新

## 路由和管理

- `/maps`：地图目录；`/mods`：独立 Mod 资料库；`/mods/[slug]`：独立条目。
- `/maps/stalingrad/edit`：管理员编辑精选地图，资料持久保存到 D1 `featured_maps`，可替换 ZIP 和封面。管理员和“我的创作”均有入口。
- `/maps/[id]/edit`：作者或管理员可编辑已有投稿；每次保存回到 pending，清除原审核状态。最多 3 份待审约束包含重新送审；乐观锁防止并发覆盖。
- 替换后的旧文件保留在私有存储中，不对外提供旧路径；后续需制定有保留期限的回收策略，避免持续积累。异常提交清理会先查询数据库引用，不删除可能已经生效的上传。

## 工作台

公众无需登录即可在 `/workbench` 编辑原版单位，导出玩法 Lua 源码、保存和导入 JSON 工程。工程不上传服务器，不跨设备自动保存。Lua 需在游戏内新建玩法 Mod 并粘贴、保存、重新开私人测试局；不是一键安装包或已发布 Mod。

数据：Steam app 1022450，build 25411656，Gameplay 3992，文件日期 2026-09-19，453 个单位。SHA-256：`a572dd3db7c928cef52d981e2e74486064e4c305a2373426ff1e441870298293`。事实参数来自作者本地游戏；不复制游戏美术/程序或第三方网站源码，不宣称第三方数据拥有 MIT 授权。

参考：

- https://github.com/AdrienRmd/War_Selection_Modding ，commit `4613575d9993f075ae792970cdf78307be906548`。
- https://github.com/IbubussI/wsunitstats-static ，commit `5ec8f8dfdcd7d7e019e9475c3d1a07fe284d7b0c`，只读参考，未发现仓库级 LICENSE。
- https://wsunitstats.com/cn/units ，参考入口，不宣称与网站当前在线数据逐项一致。

当前速度 ×16；护甲使用 deathability.armor.zonal；航空伤害 tag 14，舰船 tag 16。价格写在生产者的 work 成本阶段，不猜测 unit.cost。只允许可信内置 catalog 的现有字段。JSON 导入严格白名单且限定当前数据哈希，不接受路径/代码/自定义 catalog。全部字段预检查后再写入，攻击缓存及修改后类型重建；使用绝对赋值，非反复倍增。

v6 现已支持建筑价格、单位数量前置、已有科技队列的价格/时间，以及已有技能触发与持续时间。建筑总价为 initCost + buildCost，按 Build ID 写入所有现存阵营。最大数量是原版 live/constructed 条件门槛，不是强制限制并行队列的硬上限；超过四条或原本 any-of 的单位条件暂不开放。研究前置原样保留。

不支持新增单位、按钮、模型、弹道、目标标签、重写科技效果或动态游戏逻辑。没有进行真实游戏执行验收，原地图 Mod 不变。斯大林格勒技术资料依然标记为 2026-09-09 历史快照，与工作台原版数据分开。

当前转换：回血、采集和治疗每秒值 ×50；人口 ×10；资源/时间/距离通常 ×1000；交资源倍率 ×65536 并单独四舍五入。角度和地基生命参数保留原始值，不沿用旧角度或实验性 HP 公式。默认冲突策略为停止预检；高级覆盖也不绕过字段类型和目标身份校验。应用阶段异常明确提示重开，不声称可以自动回滚引擎集合操作。

只读取本地官方 gameplay.json 和本地运行时结构参考，数据扩展器为 scripts/expand-workbench.mjs。源码级核对和 Lua 模拟对象测试都不等于真实游戏测试。

## 中国大陆访问：仍未完成

当前 chatgpt.site 入口可能在到达应用前拦截；网站换登录按钮不能解决。用户没有其他托管账号，本次不购买服务、不注册账号、不迁移生产，也不提供绕过入口限制的方法。

迁移前须选定可供国内测试的主机/托管平台并确认账号、费用和域名条件。迁移内容包括服务端、数据库、文件存储与独立身份认证；不能把 Sites 受信身份头直接搬到公开服务器，必须防伪造且重新确认管理员归属。免费默认域名并不等于可长期稳定国内访问。需要国内不同网络真实测试，不能在境外测试后宣称国内可用。

官方平台限制参考：https://pages.edgeone.ai/document/domain-overview 、https://docs.cloudbase.net/hosting/quick-start 。本轮未实施独立邮箱/手机登录。
